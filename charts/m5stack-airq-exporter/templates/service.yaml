apiVersion: v1
kind: Service
metadata:
  name: {{ include "airq-exporter.fullname" . }}
  labels:
    {{- include "airq-exporter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "airq-exporter.selectorLabels" . | nindent 4 }}
